# Configuration file for training of Protein
# classification model
data_path: '../../input/random_split'
batch_size: 256
n_labels: 200
max_length: 100

optimizer:
    type: 'SGD'
    args:
        lr: 0.1

scheduler:
    step_size: 2
    gamma: 0.1

model:
    type: 'SimpleProteinClass'
    args:
        embed_dim: 40

trainer:
    save_dir: 'train/simple_40embed'
    epochs: 20
    save_period: 5
    monitor: 'max accuracy'
    clip_grad_norm: 5.0
    n_gpu: 1